import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lekki.com',
  appName: 'lekki',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
